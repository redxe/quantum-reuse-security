# Repository placement

Copy the contents of this directory into:

```text
docs/paper/v0.7.0/
```

Recommended repository actions:

1. Keep the PDF, LaTeX sources, figures, data, scripts, audit output, and release notes together.
2. Update the repository README to point to `docs/paper/v0.7.0/Vi_Connelly_Known_State_Qubits_Research_Draft_v0_7.pdf`.
3. Use `RELEASE_NOTES_v0.7.0.md` for the GitHub release description.
4. Upload the PDF and the clean source package ZIP as release assets.
5. Do not commit generated LaTeX auxiliaries such as `.aux`, `.log`, `.out`, `.toc`, `.fls`, or `.fdb_latexmk`.
